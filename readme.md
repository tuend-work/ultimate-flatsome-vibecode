Plugin này sẽ tạo ra các shortcode element uxbuilder cơ bản cho từng thẻ html như: div, p, i, span,a,h1,h2,h3,h4,h5,h6,li,ul,ol,table,tr,td,th,b,strong,em,u,hr,br,img,...

Dữ liệu của các trường nhập vào sẽ có thể gồm nhiều kiểu như: người dùng tự nhập, lấy từ WP post meta, ACF field,...
Có thêm trường css để tuỳ chỉnh style cho thẻ đó..

Cổng api upload attachment
cổng api UXB POST bằng token của từng user để gọi đến và tải các shortcode lên api của trang đó. Có thể tạo nhiều page. 


Viết thêm 1 skill tạo landing page từ ảnh, mô tả,... sau đó tự tải các shortcode lên api của trang đó từ Antigravity.